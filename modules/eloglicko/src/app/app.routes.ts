import { RouterModule } from '@angular/router';
import { AppComponent } from './app.component';

export const EloglickoRouting = RouterModule.forChild([
  {
    path: 'eloglicko',
    component: AppComponent,
  },
]);
