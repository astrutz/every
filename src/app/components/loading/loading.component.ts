import { Component } from '@angular/core';

/**
 * Global loading component displayed as an overlay while loading backend data
 */
@Component({
  selector: 'every-loading',
  templateUrl: 'loading.component.html',
})
export class LoadingComponent {}
